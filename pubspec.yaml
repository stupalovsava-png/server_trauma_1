name: server_trauma_1
description: A server app using the shelf package and Docker.
version: 1.0.0


environment:
  sdk: ^3.11.4

dependencies:
  relic: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
